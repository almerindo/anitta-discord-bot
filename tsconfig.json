{
  "compilerOptions": {
    /* Basic Options */
    "target": "ES2020",                       /* Define a versão do ECMAScript para suportar recursos mais novos. */
    "module": "commonjs",                     /* Especifica o sistema de módulos. */
    "declaration": true,                      /* Gera arquivo '.d.ts' correspondente. */
    "declarationMap": true,                   /* Gera sourcemap para o arquivo '.d.ts'. */
    "sourceMap": true,                        /* Gera arquivos de sourcemap. */
    "outDir": "./dist",                       /* Pasta de saída dos arquivos compilados. */
    "rootDir": "./src",                       /* Define a pasta raiz para os arquivos de entrada. */

    /* Strict Type-Checking Options */
    "strict": true,                           /* Habilita todas as verificações de tipo estritas. */

    /* Module Resolution Options */
    "esModuleInterop": true,                  /* Permite interoperabilidade entre CommonJS e ES Modules. */
    "skipLibCheck": true,                     /* Ignora verificação de tipos em arquivos de biblioteca para evitar erros com dependências. */
    "forceConsistentCasingInFileNames": true, /* Impede referências a arquivos com diferença de capitalização. */
    "resolveJsonModule": true                 /* Permite importar arquivos JSON. */
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "**/*.test.ts", "**/*.d.ts"]
}
